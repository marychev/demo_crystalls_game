require("cell.cell")
require("cell.active")
require("cell.change")
require("cell.remove")


-- local cell = Cell:new(nil, COLORS_SET.rand, 1, 1)
-- local active_cell = ActiveCell:new(cell)
-- local change_cell = ChangeCell:new(cell)
-- local remove_cell = RemoveCell:new(cell)

-- cell:draw()
-- active_cell:draw()
-- change_cell:draw()
-- remove_cell:draw()
